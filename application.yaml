eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: ${EUREKA_SERVER_ADDRESS:http://localhost:8762/eureka}


okta:
  oauth2:
    issuer: https://dev-86198069.okta.com/oauth2/default
    audience: api://default
